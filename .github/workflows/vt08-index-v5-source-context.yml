name: VT08 Index V5 Source Context Falsification

on:
  push:
    branches: [agent/vt08-index-v5-source-context-001]
    paths:
      - .github/workflows/vt08-index-v5-source-context.yml
      - src/qore/infrastructure/trader_lab/vt08_index_v5_source_context.py
      - src/qore/infrastructure/trader_lab/vt08_index_v5_context_falsification.py
      - tests/infrastructure/trader_lab/test_vt08_index_v5_source_context.py
      - tests/infrastructure/trader_lab/test_vt08_index_v5_context_falsification.py
      - docs/research/VT08-INDEX-V5-SOURCE-CONTEXT-PREREGISTRATION-001.md

concurrency:
  group: vt08-index-v5-source-context
  cancel-in-progress: false
permissions:
  actions: read
  contents: read
jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - run: ruff check .
      - run: mypy src tests
      - run: pytest -q tests/infrastructure/trader_lab/test_vt08_index_v5_source_context.py tests/infrastructure/trader_lab/test_vt08_index_v5_context_falsification.py
      - name: Full QORE gate
        run: pytest --cov=src/qore --cov-report=term-missing
  falsification:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Download frozen consumed V4 census
        uses: actions/download-artifact@v4
        with:
          name: qore-vt08-index-v4-root-cause-forensics-27d4742cda6f691b359d106c2b6fe8eb2b044fc4
          path: artifacts/v4
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          run-id: 34805546975
      - name: Execute preregistered context falsification
        run: |
          mkdir -p artifacts/v5
          python -m qore.infrastructure.trader_lab.vt08_index_v5_context_falsification --census-csv artifacts/v4/vt08-index-v4-regime-feature-census.csv --output-json artifacts/v5/vt08-index-v5-context-falsification.json
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('artifacts/v5/vt08-index-v5-context-falsification.json')
          r=json.loads(p.read_text())
          assert r['sample']==183
          assert r['contract'].startswith('consumed-evidence-only')
          PY
          printf '%s\n' "$GITHUB_SHA" > artifacts/v5/git-sha.txt
      - uses: actions/upload-artifact@v4
        with:
          name: qore-vt08-index-v5-source-context-${{ github.sha }}
          path: artifacts/v5
          if-no-files-found: error
          retention-days: 90
