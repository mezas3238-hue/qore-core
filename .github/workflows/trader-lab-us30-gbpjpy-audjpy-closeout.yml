name: QORE Trader Lab US30 GBPJPY AUDJPY Closeout

on:
  workflow_dispatch:

permissions:
  contents: read

# The original three-market run is immutable at e5c9da16ce2a1b301a39f48802fa97eef5d93ef5
# and continues independently. Automatic push relaunches are intentionally disabled
# while the validation-vault/chronological-holdout policy is frozen. The next
# automatic campaign revision will resume US30 only; GBPJPY/AUDJPY must not restart.

jobs:
  holdout-transition-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Three-market automatic relaunch disabled pending holdout freeze; existing run remains authoritative."
