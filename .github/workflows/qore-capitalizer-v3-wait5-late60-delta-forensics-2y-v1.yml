# execution checkpoint: 2026-09-23
name: QORE Capitalizer V3 WAIT5 LATE60 Delta Forensics 2Y V1

on:
  push:
    branches:
      - agent/qore-capitalizer-cognitive-v1-001
    paths:
      - '.github/workflows/qore-capitalizer-v3-wait5-late60-delta-forensics-2y-v1.yml'
      - 'src/qore/infrastructure/trader_lab/capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py'
      - 'tests/infrastructure/trader_lab/test_capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

env:
  WAIT5_RUN_ID: '35844549291'
  WAIT5_SHA: '32bd64963a34b762723416c01cf8a016fd86daab'
  LATE60_RUN_ID: '35850354903'
  LATE60_SHA: 'b8639455c31e35274dd100a650cc090da559f147'

jobs:
  forensic:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[dev]'
      - name: Ruff
        run: |
          ruff check \
            src/qore/infrastructure/trader_lab/capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py \
            tests/infrastructure/trader_lab/test_capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py
      - name: Mypy
        run: |
          mypy \
            src/qore/infrastructure/trader_lab/capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py
      - name: Focused tests
        run: |
          pytest -q --no-cov \
            tests/infrastructure/trader_lab/test_capitalizer_v3_wait5_late60_delta_forensics_2y_v1.py
      - name: Download frozen WAIT5 ledgers
        uses: actions/download-artifact@v4
        with:
          pattern: qore-capitalizer-v3-source-first-wait5-2y-v1-*-${{ env.WAIT5_SHA }}
          path: wait5-source
          merge-multiple: false
          github-token: ${{ github.token }}
          run-id: ${{ env.WAIT5_RUN_ID }}
      - name: Download frozen LATE60 ledgers
        uses: actions/download-artifact@v4
        with:
          pattern: qore-capitalizer-v3-source-first-wait5-late60-2y-v1-*-${{ env.LATE60_SHA }}
          path: late60-source
          merge-multiple: false
          github-token: ${{ github.token }}
          run-id: ${{ env.LATE60_RUN_ID }}
      - name: Run ledger delta forensics
        run: |
          python -m qore.infrastructure.trader_lab.capitalizer_v3_wait5_late60_delta_forensics_2y_v1 \
            wait5-source late60-source forensic-output
      - name: Enforce forensic controls
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          report = json.loads(
              Path(
                  "forensic-output/"
                  "capitalizer-v3-wait5-late60-delta-forensics-2y-v1.json"
              ).read_text()
          )
          assert report["identity"] == (
              "QORE_CAPITALIZER_V3_WAIT5_LATE60_DELTA_FORENSICS_2Y_V1"
          )
          assert report["wait5_raw"] == 1003
          assert report["late60_raw"] == 1368
          assert report["wait5_max3"] == 983
          assert report["late60_max3"] == 1327
          assert report["late60_added"] == 354
          assert report["wait5_displaced"] == 10
          assert report["outcome_aware"] is True
          assert report["post_result_diagnostic_only"] is True
          assert report["outcome_used_for_admission"] is False
          assert report["rule_promotion_allowed"] is False
          assert report["strategy_mutated"] is False
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: qore-capitalizer-v3-wait5-late60-delta-forensics-2y-v1-${{ github.sha }}
          path: forensic-output
          if-no-files-found: error
          retention-days: 90
