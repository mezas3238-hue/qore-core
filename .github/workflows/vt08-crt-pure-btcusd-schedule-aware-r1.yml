name: QORE VT08 CRT PURE BTCUSD Schedule-Aware R1

on:
  workflow_dispatch:
  push:
    branches:
      - agent/vt08-crt-pure-source-identity-001
    paths:
      - '.github/workflows/vt08-crt-pure-btcusd-schedule-aware-r1.yml'
      - 'src/qore/infrastructure/trader_lab/vt08_crt_pure_ctrader_market_calendar.py'
      - 'src/qore/infrastructure/trader_lab/vt08_crt_pure_btcusd_schedule_aware_replay.py'
      - 'tests/infrastructure/trader_lab/test_vt08_crt_pure_ctrader_market_calendar.py'

permissions:
  contents: read

env:
  QORE_CTRADER_CLIENT_ID: ${{ secrets.QORE_CTRADER_CLIENT_ID || secrets.QORE_CTRADER_DEMO_CLIENT_ID }}
  QORE_CTRADER_CLIENT_SECRET: ${{ secrets.QORE_CTRADER_CLIENT_SECRET || secrets.QORE_CTRADER_DEMO_CLIENT_SECRET }}
  QORE_CTRADER_ACCESS_TOKEN: ${{ secrets.QORE_CTRADER_ACCESS_TOKEN || secrets.QORE_CTRADER_DEMO_ACCESS_TOKEN }}
  QORE_CTRADER_REFRESH_TOKEN: ${{ secrets.QORE_CTRADER_REFRESH_TOKEN || secrets.QORE_CTRADER_DEMO_REFRESH_TOKEN }}
  QORE_CTRADER_DEMO_ACCOUNT_ID: ${{ secrets.QORE_CTRADER_DEMO_ACCOUNT_ID || secrets.QORE_CTRADER_ACCOUNT_ID }}

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -e '.[ctrader,dev]'
      - name: Ruff
        run: |
          ruff check \
            src/qore/infrastructure/trader_lab/vt08_crt_pure_ctrader_market_calendar.py \
            src/qore/infrastructure/trader_lab/vt08_crt_pure_btcusd_schedule_aware_replay.py \
            tests/infrastructure/trader_lab/test_vt08_crt_pure_ctrader_market_calendar.py
      - name: Mypy
        run: |
          mypy \
            src/qore/infrastructure/trader_lab/vt08_crt_pure_ctrader_market_calendar.py \
            src/qore/infrastructure/trader_lab/vt08_crt_pure_btcusd_schedule_aware_replay.py
      - name: Pytest
        run: pytest -q --no-cov tests/infrastructure/trader_lab/test_vt08_crt_pure_ctrader_market_calendar.py

  replay:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install cTrader runtime
        run: python -m pip install -e '.[ctrader]'
      - name: Discover DEMO account when needed
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${QORE_CTRADER_DEMO_ACCOUNT_ID:-}" ]; then
            account_id="$(python -m qore.infrastructure.ctrader_demo_account_discovery)"
            echo "::add-mask::$account_id"
            printf 'QORE_CTRADER_DEMO_ACCOUNT_ID=%s\n' "$account_id" >> "$GITHUB_ENV"
          fi
      - name: Run schedule-aware BTCUSD replay
        run: |
          set -euo pipefail
          mkdir -p artifacts/BTCUSD
          python -m qore.infrastructure.trader_lab.vt08_crt_pure_btcusd_schedule_aware_replay \
            artifacts/BTCUSD | tee artifacts/BTCUSD/stdout.log
      - name: Bind hashes
        run: |
          printf '%s\n' "$GITHUB_SHA" > artifacts/BTCUSD/git-sha.txt
          cd artifacts/BTCUSD
          sha256sum report.json trades.jsonl stdout.log git-sha.txt > SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: qore-vt08-crt-pure-btcusd-schedule-aware-${{ github.sha }}
          path: artifacts/BTCUSD
          if-no-files-found: error
          retention-days: 90
