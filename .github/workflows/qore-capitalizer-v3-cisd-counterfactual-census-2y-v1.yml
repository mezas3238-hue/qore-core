name: QORE Capitalizer V3 CISD Counterfactual Census 2Y V1

on:
  push:
    branches:
      - agent/qore-capitalizer-cognitive-v1-001
    paths:
      - '.github/workflows/qore-capitalizer-v3-cisd-counterfactual-census-2y-v1.yml'
      - 'src/qore/infrastructure/trader_lab/capitalizer_v3_cisd_counterfactual_census_2y_v1.py'
      - 'tests/infrastructure/trader_lab/test_capitalizer_v3_cisd_counterfactual_census_2y_v1.py'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: qore-capitalizer-v3-cisd-counterfactual-census-2y-v1-${{ github.sha }}
  cancel-in-progress: false

env:
  BOTTLENECK_RUN_ID: '35804015774'
  BOTTLENECK_SHA: '8cd1f83221f8e89c9586f0a241e18f91ad5ac0fc'
  MICRO_RUN_ID: '35805397236'
  MICRO_SHA: 'e3be1ed5dda60ab3a53257287c9471ce247d2554'
  SOURCE_M1_RUN_ID: '35548099334'
  SOURCE_M1_SHA: '18c338aedd5013ce65a6cb6408ffbc2e904a6217'

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[dev]'
      - name: Ruff
        run: |
          ruff check \
            src/qore/infrastructure/trader_lab/capitalizer_v3_cisd_counterfactual_census_2y_v1.py \
            tests/infrastructure/trader_lab/test_capitalizer_v3_cisd_counterfactual_census_2y_v1.py
      - name: Mypy
        run: |
          mypy src/qore/infrastructure/trader_lab/capitalizer_v3_cisd_counterfactual_census_2y_v1.py
      - name: Focused tests
        run: |
          pytest -q --no-cov \
            tests/infrastructure/trader_lab/test_capitalizer_v3_cisd_counterfactual_census_2y_v1.py

  market:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - {symbol: USDJPY, session: ASIA}
          - {symbol: AUDJPY, session: ASIA}
          - {symbol: AUDUSD, session: ASIA}
          - {symbol: GBPJPY, session: ASIA}
          - {symbol: EURUSD, session: LONDON}
          - {symbol: GBPUSD, session: LONDON}
          - {symbol: XAUUSD, session: NEW_YORK}
          - {symbol: USDCAD, session: NEW_YORK}
          - {symbol: NAS100, session: NEW_YORK}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e .
      - name: Download frozen 2Y bottleneck cell
        uses: actions/download-artifact@v4
        with:
          name: qore-capitalizer-m3-mss-bottleneck-forensics-2y-v1-${{ matrix.symbol }}-${{ env.BOTTLENECK_SHA }}
          path: bottleneck-source
          github-token: ${{ github.token }}
          run-id: ${{ env.BOTTLENECK_RUN_ID }}
      - name: Download frozen 2Y microstructure cell
        uses: actions/download-artifact@v4
        with:
          name: qore-capitalizer-v3-m3-microstructure-context-2y-v1-${{ matrix.symbol }}-${{ env.MICRO_SHA }}
          path: micro-source
          github-token: ${{ github.token }}
          run-id: ${{ env.MICRO_RUN_ID }}
      - name: Download immutable native-M1 clone
        uses: actions/download-artifact@v4
        with:
          name: qore-capitalizer-cibo-10y-native-m1-${{ matrix.symbol }}-${{ env.SOURCE_M1_SHA }}
          path: m1-source
          github-token: ${{ github.token }}
          run-id: ${{ env.SOURCE_M1_RUN_ID }}
      - name: Run CISD counterfactual census
        run: |
          python -m qore.infrastructure.trader_lab.capitalizer_v3_cisd_counterfactual_census_2y_v1 \
            market bottleneck-source micro-source m1-source census-output
      - name: Enforce census invariants
        env:
          EXPECTED_SYMBOL: ${{ matrix.symbol }}
          EXPECTED_SESSION: ${{ matrix.session }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          symbol = os.environ["EXPECTED_SYMBOL"]
          path = Path("census-output") / (
              f"capitalizer-{symbol.lower()}-v3-cisd-counterfactual-census-2y-v1.json"
          )
          report = json.loads(path.read_text())
          assert report["identity"] == "QORE_CAPITALIZER_V3_CISD_COUNTERFACTUAL_CENSUS_2Y_V1"
          assert report["symbol"] == symbol
          assert report["session"] == os.environ["EXPECTED_SESSION"]
          assert report["cisd_first_blockers"] == (
              report["same_bar_cisd_only"]
              + report["distributed_non_cisd_components_only"]
              + report["non_cisd_component_incomplete"]
          )
          assert report["same_bar_cisd_only"] == (
              report["same_bar_cisd_only_with_cisd_elsewhere"]
              + report["same_bar_cisd_only_without_cisd_elsewhere"]
          )
          assert report["missing_microstructure_rows"] == 0
          assert report["m5_state_mapping_frozen"] is False
          assert report["m5_state_mapping_deferred_to_step2"] is True
          assert report["strategy_mutated"] is False
          assert report["thresholds_mutated"] is False
          assert report["outcome_used_for_admission"] is False
          assert report["diagnostic_only"] is True
          assert report["economic_candidate"] is False
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: qore-capitalizer-v3-cisd-counterfactual-census-2y-v1-${{ matrix.symbol }}-${{ github.sha }}
          path: census-output
          if-no-files-found: error
          retention-days: 90

  aggregate:
    needs: market
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e .
      - uses: actions/download-artifact@v4
        with:
          pattern: qore-capitalizer-v3-cisd-counterfactual-census-2y-v1-*-${{ github.sha }}
          path: census-input
          merge-multiple: false
      - name: Build nine-market CISD census
        run: |
          python -m qore.infrastructure.trader_lab.capitalizer_v3_cisd_counterfactual_census_2y_v1 \
            matrix census-input matrix-output
      - name: Enforce aggregate controls
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          report = json.loads(
              Path(
                  "matrix-output/"
                  "capitalizer-nine-market-v3-cisd-counterfactual-census-2y-v1.json"
              ).read_text()
          )
          assert report["identity"] == (
              "QORE_CAPITALIZER_NINE_MARKET_V3_CISD_COUNTERFACTUAL_CENSUS_2Y_V1"
          )
          assert report["market_count"] == 9
          assert report["cisd_control_reproduced"] is True
          assert report["cisd_first_blockers"] == 3007
          assert report["cisd_first_blockers"] == (
              report["same_bar_cisd_only"]
              + report["distributed_non_cisd_components_only"]
              + report["non_cisd_component_incomplete"]
          )
          assert report["same_bar_cisd_only"] == (
              report["same_bar_cisd_only_with_cisd_elsewhere"]
              + report["same_bar_cisd_only_without_cisd_elsewhere"]
          )
          assert report["missing_microstructure_rows"] == 0
          assert report["m5_state_mapping_frozen"] is False
          assert report["m5_state_mapping_deferred_to_step2"] is True
          assert report["strategy_mutated"] is False
          assert report["thresholds_mutated"] is False
          assert report["outcome_used_for_admission"] is False
          assert report["diagnostic_only"] is True
          assert report["economic_candidate"] is False
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: qore-capitalizer-nine-market-v3-cisd-counterfactual-census-2y-v1-${{ github.sha }}
          path: matrix-output
          if-no-files-found: error
          retention-days: 90
