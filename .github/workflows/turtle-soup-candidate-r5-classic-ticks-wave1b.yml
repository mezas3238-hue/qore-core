name: QORE Turtle Soup R5 Classic Tick Resolution Wave 1B

on:
  push:
    branches:
      - agent/turtle-soup-candidate-r5-classic-tick-resolution-001
    paths:
      - .github/workflows/turtle-soup-candidate-r5-classic-ticks-wave1b.yml
      - src/qore/infrastructure/trader_lab/turtle_soup_candidate_r5_classic_tick_runtime.py
      - src/qore/infrastructure/trader_lab/turtle_soup_candidate_r5_classic_tick_probe.py
      - src/qore/infrastructure/trader_lab/turtle_soup_candidate_r5_classic_tick_wave1b_targets.py

concurrency:
  group: qore-turtle-soup-r5-classic-ticks-wave1b-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  QORE_CTRADER_CLIENT_ID: ${{ secrets.QORE_CTRADER_CLIENT_ID || secrets.QORE_CTRADER_DEMO_CLIENT_ID }}
  QORE_CTRADER_CLIENT_SECRET: ${{ secrets.QORE_CTRADER_CLIENT_SECRET || secrets.QORE_CTRADER_DEMO_CLIENT_SECRET }}
  QORE_CTRADER_ACCESS_TOKEN: ${{ secrets.QORE_CTRADER_ACCESS_TOKEN || secrets.QORE_CTRADER_DEMO_ACCESS_TOKEN }}
  QORE_CTRADER_REFRESH_TOKEN: ${{ secrets.QORE_CTRADER_REFRESH_TOKEN || secrets.QORE_CTRADER_DEMO_REFRESH_TOKEN }}
  QORE_CTRADER_DEMO_ACCOUNT_ID: ${{ secrets.QORE_CTRADER_DEMO_ACCOUNT_ID || secrets.QORE_CTRADER_ACCOUNT_ID }}

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        symbol: [GBPUSD, USDCAD]
    env:
      QORE_TURTLE_SOUP_SYMBOL: ${{ matrix.symbol }}
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install cTrader runtime
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[ctrader]"

      - name: Verify required read-only inputs
        shell: bash
        run: |
          set -euo pipefail
          for name in QORE_CTRADER_CLIENT_ID QORE_CTRADER_CLIENT_SECRET QORE_CTRADER_ACCESS_TOKEN QORE_CTRADER_REFRESH_TOKEN QORE_TURTLE_SOUP_SYMBOL; do
            if [ -z "${!name:-}" ]; then
              echo "missing required market-data input: ${name}" >&2
              exit 2
            fi
          done

      - name: Discover authorized read-only cTrader account when not configured
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${QORE_CTRADER_DEMO_ACCOUNT_ID:-}" ]; then
            account_id="$(python -m qore.infrastructure.ctrader_demo_account_discovery)"
            if ! [[ "$account_id" =~ ^[1-9][0-9]*$ ]]; then
              echo "cTrader account discovery returned an invalid id" >&2
              exit 3
            fi
            echo "::add-mask::$account_id"
            printf 'QORE_CTRADER_DEMO_ACCOUNT_ID=%s\n' "$account_id" >> "$GITHUB_ENV"
          fi

      - name: Collect omitted BID tick minute
        run: |
          set -euo pipefail
          dir="artifacts/turtle-soup-r5-classic-ticks-wave1b/${QORE_TURTLE_SOUP_SYMBOL}"
          mkdir -p "$dir"
          python -m qore.infrastructure.trader_lab.turtle_soup_candidate_r5_classic_tick_runtime \
            --target-wave wave1b \
            --symbol "$QORE_TURTLE_SOUP_SYMBOL" \
            --software-sha "$GITHUB_SHA" \
            --output "$dir/tick-evidence.json"
          printf '%s\n' "$GITHUB_SHA" > "$dir/git-sha.txt"

      - name: Enforce Wave 1B governance
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          symbol = os.environ["QORE_TURTLE_SOUP_SYMBOL"]
          path = Path(f"artifacts/turtle-soup-r5-classic-ticks-wave1b/{symbol}/tick-evidence.json")
          p = json.loads(path.read_text(encoding="utf-8"))
          assert p["read_only"] is True
          assert p["research_only"] is True
          assert p["fresh_oos_consumed"] is False
          assert p["research_identity"] == "turtle-soup-candidate-r5"
          assert p["quote_type"] == "BID"
          assert p["symbol"] == symbol
          assert p["software_sha"] == os.environ["GITHUB_SHA"]
          assert p["target_count"] == 1
          assert p["available_target_count"] + p["unavailable_target_count"] == 1
          assert len(p["target_manifest_digest_sha256"]) == 64
          assert len(p["evidence_digest_sha256"]) == 64
          PY

      - name: Upload immutable Wave 1B evidence
        uses: actions/upload-artifact@v4
        with:
          name: qore-turtle-soup-r5-classic-ticks-wave1b-${{ matrix.symbol }}-${{ github.sha }}
          path: artifacts/turtle-soup-r5-classic-ticks-wave1b/${{ matrix.symbol }}
          if-no-files-found: error
          retention-days: 30
