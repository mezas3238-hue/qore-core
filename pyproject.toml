[tool.ruff]
line-length = 100
select = ["E", "F", "I", "N", "W"]

[tool.mypy]
python_version = "3.12"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=domain --cov=application --cov-report=term --cov-report=html"

[tool.import-linter]
root_package = "qore"
