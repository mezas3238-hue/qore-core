# VT-08 Index R1 — Formal Validation Rejection

Checkpoint: 2026-09-13

## Decision

`VT08_INDEX_C2_POSITIONAL_R1` is `R1_REJECTED_FOR_PROMOTION`.

`DEMO_ELIGIBLE=false`, `LIVE_AUTHORIZED=false`, and
`PRODUCTION_AUTHORIZED=false`.

The exact R1 replay and repaired immutable forensics reconcile, but the evidence does
not support a robust positive edge. The second chronological half is negative, only two
of four quartiles have positive mean R, leave-NAS100-out is negative, leave-06:00-out is
negative, and every 10,000-path block bootstrap has a non-positive P05 mean-R tail.

## Source-fidelity re-adjudication

R1 already applies the mechanically supported narrow combination of completed C2,
previous-H4-extreme POI, M15 CISD, exactly one Protected Swing, positional entry at the
new H4 open, Protected Swing invalidation, and the author-supported initial 2R objective.
The author material still does not supply a universal mechanical priority for multiple
POIs, multiple Protected Swings, qualitative shallow/large wick classification, or
contextual objectives. No source defect has therefore been proven that authorizes an R2.

R1 market, anchor, side, year and CIBO results are consumed diagnostics. They cannot be
promoted into NAS100-only, 06:00-only, SHORT-only, or retrospectively selected CIBO
rules.

## Fresh-data preservation

The 2024-08-13 through 2026-09-11 interval is consumed. Because R1 fails the robustness
gate before independent validation, no new holdout is opened for it. Preserving unseen
data avoids spending the next validation set on an already-rejected candidate.

Any future R2 requires new same-author source evidence, a distinct identity and a
pre-economic freeze. Only after that freeze may an unused dataset be fingerprinted and
opened. CIBO remains a request layer and Risk remains the final capital authority.
