# VT08 CRT PURE — KEY LEVEL SOURCE CLOSURE 001

**Status:** ROLE CLOSED / MINIMAL SOURCE-NATIVE FAMILY SET CLOSED / GEOMETRY PARTIAL  
**PR:** #626  
**Date:** 2026-09-22

## Primary-source closures

RomeoTPT directly states that a trader can:

- trade the Journey of price **to** a key level; or
- trade the reaction of price **from** a key level.

Primary locator:
https://t.me/s/officialRomeotpt?before=6280

RomeoTPT separately defines:

- an ideal bearish reaction as a candle opening, stabbing an **old CRTH**, then dumping;
- the inverse old-CRL family is retained symmetrically for bullish source representation;
- **MOB** as the make-or-break level: break through it and the target remains open; respect it
  and the target is blocked / the idea should be scratched.

Primary locators:
https://t.me/s/officialRomeotpt/6615
https://t.me/s/officialRomeotpt?before=6745

## Minimal source-native family set

The current code may name only:

- `OLD_CRTH`
- `OLD_CRTL`
- `MOB`
- `OTHER_SOURCE_AUTHORIZED` for future primary closure

This is intentionally narrower than Level-B lists.

## Explicit non-closures

The reviewed primary evidence does **not** authorize us to freeze all of the following as
universal CRT key levels:

- generic Order Block;
- generic FVG;
- PDH/PDL;
- PWH/PWL;
- session high/low;
- support/resistance;
- supply/demand.

Those concepts may exist elsewhere in Romeo's teaching, but they are not admitted here until
their CRT key-level role is closed from accepted primary evidence.

The source also does not close:

- a universal distance/proximity threshold;
- a universal touch-count rule;
- a universal nearest-level priority;
- a universal `NO_KEY_LEVEL => ABSTAIN`;
- a numeric MOB detector.

## Cognitive contract

Key-level context can classify:

- `JOURNEY_TO`
- `REACTION_FROM`
- `MAKE_OR_BREAK`
- `UNKNOWN`

It cannot independently authorize entry or capital.

Mixed/conflicting interactions collapse to `UNKNOWN`; the system does not invent priority.

## R2-B use

R2-B tests only one source-native reference family:

`prior completed parent CRT C1 high/low -> old CRTH/CRL characterization`

The family is source-native.  The ownership geometry (previous completed parent CRT,
first strict breach, untouched semantics) remains an engineering characterization and is
not promoted from PnL.

MOB remains cognition-only until a deterministic source-faithful geometry is found.
